headless.mode = false
serenity {
    take.screenshots = FOR_EACH_ACTION
}

# Chrome options can be defined using the chrome.switches property

chrome.switches = """--start-maximized; --disable-download-notification; --disable-gpu"""

environments {
    default {
    webdriver.base.url = "https://www.surveymonkey.com/r/9MVSPYS"}
}
